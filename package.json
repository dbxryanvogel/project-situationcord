{
  "name": "project-situationcord",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.0.0",
    "turbo": "^2.0.0"
  },
  "packageManager": "bun@1.3.3",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@neondatabase/auth": "^0.1.0-beta.20"
  }
}
